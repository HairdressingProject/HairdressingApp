import { createBrowserHistory } from 'history';

/**
 * @var {History<{}>} history - Creates a new browser history, needed for react-router to work properly
 */
export const history = createBrowserHistory();